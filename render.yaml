services:
  - type: web
    name: intel-retail-backend
    runtime: python
    region: ohio
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_Ii6SfwKHJA7B@ep-fancy-waterfall-a5fta2du.us-east-2.aws.neon.tech/neondb?sslmode=require
      - key: PYTHON_VERSION
        value: 3.11.0
